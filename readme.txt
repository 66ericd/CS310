You can run the website by running app.py, it should then be available at http://127.0.0.1:5000/ (localhost)
Packages required: Flask, AIF360, Pandas, NumPy, Scikit-learn and any of their dependencies.
Cleaned version of Adult Income dataset can be found in the datasets folder.